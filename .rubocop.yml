inherit_gem:
  rubocop-rails-omakase: rubocop.yml

AllCops:
  NewCops: enable
  TargetRubyVersion: 3.3
  Exclude:
    - "bin/*"
    - "db/schema.rb"
    - "node_modules/**/*"
    - "vendor/**/*"

require:
  - rubocop-rails
  - rubocop-rspec

Layout/LineLength:
  Max: 120

Rails:
  Enabled: true
