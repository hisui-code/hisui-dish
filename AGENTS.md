# AGENTS.md（HisuiDish / 学習用）

このリポジトリは **学習目的** です。  
「動けばいい」ではなく、**設計・分離・可読性を学ぶためのコード**
を目指します。

---

# 🎯 最優先方針

## 1. 可読性と責務の分離を最優先にする

- **最小差分よりも、分離と読みやすさを優先する**
- ただし、分離しすぎて読みにくくなる設計はNG
- 「1ファイルに詰め込みすぎ」も「細かく分けすぎ」も避ける
- 抽象化は“必要になったとき”に行う

### 判断基準

- そのコードは **初見で読めるか？**
- その関数は **1つの責務だけを持っているか？**
- 分離したことで **逆に追いづらくなっていないか？**

---

## 2. コード編集と更新のルール

- **コード編集はユーザー本人が手打ちで行う**
- **Codex は、明示的に依頼された場合を除き、ファイルを直接編集してはならない**
- **明示的な指示がない限り、コード・設定・依存関係の更新を禁止する**
- Codex の標準動作は「変更案・差分案・貼り付け用コードの提示のみ」とする
- 自動編集ツールの使用は、ユーザーが明示的に許可した場合のみ可
- 「編集して」「反映して」「ファイルを直接更新して」などの明示がある場合のみ、Codex はファイルを編集してよい
- 「案を出して」「レビューして」「どう直す？」は、編集許可ではなく提案依頼として扱う

---

## 3. コマンド実行のルール

- 調査・確認のための read-only コマンドは実行してよい
- ファイル生成・自動修正・依存更新を伴うコマンドは、明示的な許可がある場合のみ実行する
- テスト実行で生成物が出る場合は、影響範囲を確認してから行う

---

## 4. 依存関係の扱い（更新・導入）

- npm / composer のパッケージ更新を自動では行わない
- Laravel / React のメジャーアップデートは行わない
- lockfile 更新のみのPRは禁止

### 方針

- 必要に応じて「提案」はしてよい
- **自己判断で導入・更新するのは禁止**
- 導入が必要な場合は以下を提示すること：
  - なぜ必要か
  - 代替案はあるか
  - 影響範囲

---

## 5. ドキュメントコメントは“概要必須”

### 方針

- **概要（このコードは何のために存在するか）は必須**
- @param / @return は重要な場合のみでOK
- 型だけで自明なものは簡潔でよい
- 意図を書くことを重視する

---

# 📝 ドキュメントコメントルール

## フロント（JS / TS）

- 重要な関数・API呼び出し・日付処理にはJSDocを書く
- `@description` は必須
- 必要に応じて `@param` / `@returns`
- 型エイリアスや props 型の各プロパティには `@description` などのタグを使わない
- 型のプロパティコメントはホバー時の可読性を優先してプレーンな JSDoc を使う

例：

```ts
/**
 * @description JST境界の期間をUTCに変換するユーティリティ。
 * APIクエリはUTCで統一しているため、この変換を行う。
 */
export function buildUtcRange(startJst: Date, endJst: Date) {
  // JSTで受け取った日時をUTCに変換する
  // DBはUTC保存前提
}

type HealthLogSummaryCardsProps = {
  /** 最後の記録の日付表示 */
  latestLogDate: string
}
```

---

## バックエンド（Laravel / PHP）

- Controller / Service / 集計ロジックにはPHPDocを書く
- 概要は必須
- 戻り値の構造が重要な場合は具体的に書く

例：

```php
/**
 * JST境界をUTCに変換し、DB検索用の期間を返す。
 */
function buildUtcRange(DateTimeImmutable $startJst, DateTimeImmutable $endJst): array
{
    // JSTからUTCへ変換する
    // DBはUTC保存のため、この範囲で検索する
}
```

---

# 💬 処理コメントのルール

- 複雑な分岐・日付境界・集計処理には必ずコメントを書く
- 「何をしているか」＋「なぜそれをするのか」を書けると良い
- コメントの文末に「。」は付けない
- 日本語で書く

---

# 🧱 設計方針

## 責務の分離

- データ取得
- バリデーション
- 集計
- 表示整形

これらは可能な限り分離する

ただし：

- 3行程度のロジックを無理に別関数にしない
- 追いづらくなる分離はしない

---

## 後方互換を守る

- 既存挙動は壊さない
- 変更が必要な場合は：
  - 影響範囲
  - 代替案
  - 移行方法 を先に提示する

---

## 仕様変更時の流れ

1.  `docs/spec/` を先に修正
2.  `docs/spec/90_decisions.md` に理由を書く
3.  その後に実装する

---

# 🛠 作業の進め方

1.  目的を1〜3行で宣言（何を・なぜ）
2.  影響範囲を列挙（API / DB / UI / 集計）
3.  設計を説明（責務の分け方）
4.  実装方針を提示
5.  動作確認手順を書く

---

# 🧭 Skill routing / Skills の使い分け

Codex は、相談内容に該当する skill だけを確認する。
全 skill を毎回読む必要はない。

## React / TypeScript

- React / TypeScript の性能改善、bundle、再レンダー、データ取得の相談は `.agents/skills/vercel-react-best-practices/SKILL.md` を確認する
- コンポーネント設計、props 設計、責務分離、composition の相談は `.agents/skills/vercel-composition-patterns/SKILL.md` を確認する

## UI / UX

- UI / UX / accessibility のレビューは `.agents/skills/web-design-guidelines/SKILL.md` を確認する

## Docs / Writing

- docs や文章のレビューは `.agents/skills/writing-guidelines/SKILL.md` を確認する

## Vercel

- Vercel の本番環境のコスト・性能調査は `.agents/skills/vercel-optimize/SKILL.md` を確認する

## 読む範囲の原則

- 該当する skill だけを読む
- 複数領域にまたがる場合だけ、必要な skill を組み合わせて読む
- 判断に迷う場合は、最も影響が大きい領域の skill を優先する
- skill の確認は設計・提案・レビューの精度を上げるために行い、不要なファイル探索は増やさない

---

# 🗣 回答言語

- 原則として日本語で回答する
- コード、コマンド、エラー文、技術用語は必要に応じて英語のまま扱う

---

# 💻 コーディング規約

## フロント（React / TypeScript）

- React / TypeScript の提案時は、上記の Skill routing に従って必要な skill だけを確認する
- コンポーネントは `function` 宣言
- 内部関数はアロー関数
- 画面は `Page`、表示は `components`、ロジックは `hooks` / `lib` に分離する
- 型のプロパティコメントは `/** ... */` を使い、`@description` などのタグは使わない

---

## バックエンド（Laravel / PHP）

- バリデーションは統一した形式で返す
- エラーレスポンス形式を揃える
- 戻り値の構造が重要な箇所はPHPDocで明示する

---

# 🧪 テスト・確認

- 変更した箇所は最低1つ確認を書く
- 自動テストがなければ手動確認手順を書く
- タイムゾーン処理はJST 0時付近で必ず確認する

---

# 🧾 Git / コミット

- コミットは手動
- コミットメッセージは提示する
- 必要なら分割案も提示する
- docs/commit-rules.md を確認する

---

# 🚫 禁止事項

- 仕様外の“ついで変更”
- 推測で仕様を埋める
- 不要な抽象化
- 自己判断での依存追加・更新

---

# 🎓 このリポジトリの目的

- フルスタックの設計力を身につける
- タイムゾーン・集計・境界処理を正しく扱えるようになる
- 「読めるコード」を書けるようになる
